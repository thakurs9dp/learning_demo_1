package com.s9dp.permssiononandroid.coroutines

class Coroutines {
    //Coroutines is frame work and it is a light weight threads that run the code as asynchronously


    // Coroutines Scope (Lifetime) (Activity, fragment and viewmodel) coroutines scope define the lifecycle
    // Coroutines Context and Dispatcher (Thread) (Dispatchers is way to define threads on which coroutines are executed.






}